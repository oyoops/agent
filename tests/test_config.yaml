app:
  name: AI Web App Test
  debug: true

server:
  host: 127.0.0.1
  port: 5000

ai:
  model_name: test_model
  max_tokens: 100
  temperature: 0.7

features:
  enable_data_analysis: true
  enable_recommendations: true
  enable_sentiment_analysis: true

logging:
  level: DEBUG
  format: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'